<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.it.lylj.email.model.EmailDAO">
	<!-- 검색조건 -->	
	<sql id="searchWhere">
	   <if test="searchKeyword!=null and searchKeyword!=''">
		 and ${searchCondition}  like '%' || #{searchKeyword} || '%'
	   </if>
	</sql>

	<insert id="sendEmail" parameterType="EmailVo">
		<selectKey keyProperty="mailNo" resultType="int" order="BEFORE">
			select mail_seq.nextval from dual
		</selectKey>
		insert into mail 
		values (#{mailNo}, #{mailTitle},#{mailContent}, #{mailSend}, #{mailTake}, default,null,null,default, #{mailEmpno})
	</insert>
	
	<parameterMap type="" id=""></parameterMap>
	
	<select id="selectMailList" parameterType="String" resultType="map">
		select *
		from
		(
		    select rownum as RNUM, A.*
		    from
		    (
		       select * from emailView
		       where mail_take=#{empNo}
			   <include refid="searchWhere"/>
		       order by MAIL_SENDDATE desc
		    )A
		)
		<![CDATA[
		where RNUM > #{firstRecordIndex} 
		and RNUM <= #{firstRecordIndex} + #{recordCountPerPage}
		]]>
	</select>
	
<!-- 	<select id="selectMailList" parameterType="String" resultType="map">
		select * from mailView
		where mail_take=#{empNo}
		order by MAIL_SENDDATE desc
	</select> -->
	
	<select id="selectByMailNo" parameterType="int" resultType="emailVo">
		select * from mail
		where MAIL_NO=#{mailNo}
	</select>
	
	<select id="totalRecordByEmailTake" parameterType="String" resultType="int">
		select count(*) from mailView
		where mail_take = #{mailTake} 
	</select>
</mapper>









