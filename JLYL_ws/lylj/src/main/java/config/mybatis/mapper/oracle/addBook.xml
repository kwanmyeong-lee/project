<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.it.lylj.addBook.model.AddBookDAO">
	<sql id="searchWhere">
		<if test="searchKeyword!=null and searchKeyword!=''">
			AND ${searchCondition} like '%' || #{searchKeyword} || '%'
		</if>
	</sql>
	
	<insert id="insertAddBook" parameterType="AddBookVO">
		<selectKey keyProperty="addressBookNo" resultType="int" order="BEFORE">
			SELECT ADDBOOK_SEQ.nextval FROM dual
		</selectKey>
		INSERT INTO addbook(ADDRESSBOOK_NO, EMP_NO, ADDRESSBOOK_NAME, ADDRESSBOOK_TEL, ADDRESSBOOK_MAILE, ADDRESSBOOK_POSITION, ADDRESS_FOLDER_NO)
		VALUES(#{addressBookNo}, #{empNo}, #{addressBookName}, #{addressBookTel}, #{addressBookMaile}, #{addressBookPosition}, #{addressFolderNo})
	</insert>
	
	<select id="selectAllAddBook" parameterType="searchVo" resultType="addBookVo">
		SELECT *
			FROM
			(
				SELECT rownum as RNUM,A.*
				from
				(
					SELECT * FROM addbook
					WHERE EMP_NO=#{empNo}
					<include refid="searchWhere"></include>
					ORDER BY ADDRESSBOOK_NO DESC
				)A
		)
		<![CDATA[
		where RNUM > #{firstRecordIndex} 
		and RNUM <= #{firstRecordIndex} + #{recordCountPerPage}
		]]>
	</select>
	
	<select id="selectAllTotalRecord" parameterType="searchVo"
		resultType="int">
		SELECT count(*) FROM addbook
		WHERE EMP_NO=#{empNo}
		<include refid="searchWhere"></include>
	</select>
</mapper>









